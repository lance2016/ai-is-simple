# 练习项目：部署重试规则

生产部署失败后，自动流程最多重试 **2 次**。每次重试前等待服务端返回的 Retry-After；如果响应没有该字段，默认等待 **30 秒**。

认证失败（401）和参数错误（400）不重试。服务不可用（503）可以按上述规则重试。超过重试次数后，流程应停止并提示人工检查，不要继续循环。

这个练习文件是只读事实来源。回答涉及具体数字时，Agent 应先读取它；只问概念或打招呼时，不需要读取工作区。
