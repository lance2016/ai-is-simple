# 图示规范

本项目的图只承担一个任务：帮助初学者看懂概念之间的关系。

## 默认使用 Mermaid

流程、循环、分支和依赖关系优先使用 Mermaid，而不是手绘图片或 AI 生成图片。

这样做有三个好处：

- 文字清楚，不会出现生成图片里的错字；
- 箭头和分支准确，修改起来也快；
- 图可以直接放进 Markdown，和文字、代码保持在一起。

## 每张图的规则

1. 一张图只讲一个核心概念。
2. 节点名称尽量控制在 2～6 个字。
3. 先画主路径，再画循环和终止分支。
4. 用户输入、模型决策、工具执行、结果返回、最终输出要区分清楚。
5. 颜色只用于表达角色或状态，不用来装饰。
6. 图下面再用短段落补充细节，不把所有解释塞进图里。

