# 图示规范

本项目的图只承担一个任务：帮助初学者看懂概念之间的关系。

## 图示优先级

优先使用已经确认过的、简洁高级的教学图作为章节主图。Mermaid 适合快速表达流程和草稿，但同一章只保留一种核心图，不要让两张表达同一件事的图同时出现。

如果没有确认过的图片，再使用 Mermaid：

- 文字清楚，不出现错字；
- 箭头和分支准确；
- 可以直接放进 Markdown，方便后续修改。

## 每张图的规则

1. 一张图只讲一个核心概念。
2. 节点名称尽量控制在 2～6 个字。
3. 先画主路径，再画循环和终止分支。
4. 用户输入、模型决策、工具执行、结果返回、最终输出要区分清楚。
5. 颜色只用于表达角色或状态，不用来装饰。
6. 图下面再用短段落补充细节，不把所有解释塞进图里。
